# نقشه‌ی آماده‌سازیِ محصول برای تولیدِ بلندمدت

> این فایل فهرستِ کارهای بهبودِ زیرساخت است. آزادانه ترتیب/اولویت را تغییر بده،
> موارد را حذف/اضافه کن، و هر تسکی را که خواستی انجام شود علامت بزن یا به من بگو.
>
> راهنمای برچسب‌ها:
> - **اولویت:** `P0` بحرانی · `P1` مهم · `P2` خوب‌است‌داشته‌باشیم
> - **حجم:** `S` کوچک (ساعت) · `M` متوسط (روز) · `L` بزرگ (هفته+)
> - وضعیت: `[ ]` انجام‌نشده · `[~]` در حال انجام · `[x]` انجام‌شده

---

## گروه A — ایمنیِ داده و پشتیبان‌گیری  (بیشترین اهمیت برای اطمینانِ خاطر)

- [ ] **A1. پشتیبانِ کاملِ فایل‌ها، نه فقط دیتابیس** — `P0` · `M`
  - مشکل: پشتیبانِ فعلی (`server/backup.js`) فقط `tooscable.db` را می‌گیرد. امضاها، ضمیمه‌ها، ضبط‌ها و آواتارها (`data/uploads`, `sharedfiles`, `recordings`, `signatures`, `avatars`, `branding`) در پشتیبان نیستند.
  - خروجی: پشتیبان به‌صورت آرشیوِ فشرده از کلِ `data/` (فایل‌ها + اسنپ‌شاتِ DB) با نگه‌داری نسخه‌ای.
  - قبولی: بازیابیِ کاملِ سامانه فقط از روی یک آرشیو ممکن باشد.

- [ ] **A2. کپیِ خارج از سرور (offsite) — قانون ۳-۲-۱** — `P0` · `M`
  - مشکل: `data/backups` روی همان دیسکِ دیتابیس است؛ خرابیِ دیسک = نابودیِ هم‌زمانِ اصل و پشتیبان.
  - خروجی: ارسالِ خودکارِ پشتیبان به مقصدِ دوم (NAS/سرورِ دوم/فضای ابری/rsync)، حداقل یک نسخه خارج از محل.
  - قبولی: با از دست رفتنِ کاملِ سرورِ اصلی، داده از مقصدِ دوم قابل‌بازیابی باشد.

- [ ] **A3. بررسیِ صحت + رزمایشِ بازیابی** — `P0` · `M`
  - مشکل: پشتیبانِ تست‌نشده = پشتیبانِ نداشته.
  - خروجی: اجرای `PRAGMA integrity_check` روی هر نسخه، و اسکریپت/رویه‌ی بازیابیِ دوره‌ای در محیطِ جدا.
  - قبولی: گزارشِ «آخرین بازیابیِ موفق در تاریخ …».

- [ ] **A4. هشدار هنگام شکستِ پشتیبان‌گیری** — `P0` · `S`
  - مشکل: اگر پشتیبان‌گیری خطا بدهد فقط در لاگ چاپ می‌شود و کسی خبردار نمی‌شود.
  - خروجی: اعلان به مدیرِ سامانه (نوتیفیکیشن/ایمیل/پیامک) اگر بیش از X ساعت پشتیبانِ سالم ساخته نشد.

- [ ] **A5. سیاستِ نگه‌داری و آرشیوِ بلندمدت** — `P1` · `S`
  - خروجی: نگه‌داریِ لایه‌ای (روزانه‌ی اخیر + هفتگی + ماهانه + سالانه) به‌جای صرفاً «۱۴ نسخه‌ی آخر».

---

## گروه B — رمزنگاریِ داده در حالتِ سکون (at-rest)

- [ ] **B1. رمزنگاریِ فایل‌های حساس در لایه‌ی برنامه** — `P0` · `M`
  - مشکل: امضاها و برخی فایل‌ها به‌صورتِ plain روی دیسک‌اند.
  - خروجی: رمزنگاریِ امضاها (و ضمیمه‌های حساس) با `AES-256-GCM`؛ سامانه هنگام سرو رمزگشایی می‌کند.
  - قبولی: فایل‌های روی دیسک بدونِ کلید غیرقابل‌خواندن باشند، اما نمایش در سامانه بدونِ تغییرِ تجربه‌ی کاربر کار کند.

- [ ] **B2. مدیریتِ امنِ کلید (Key Management)** — `P0` · `M` · وابسته به B1
  - خروجی: نگه‌داریِ کلید خارج از مخزنِ کد (متغیر محیطی/کلیدخانه‌ی OS/KMS)، امکانِ چرخشِ کلید، و **پشتیبانِ جداگانه‌ی کلید** جدا از داده‌ها.
  - ⚠️ ریسک: گم‌شدنِ کلید = از دست رفتنِ داده. باید رویه‌ی مکتوب داشته باشد.

- [ ] **B3. رمزنگاریِ فیلدهای حساسِ دیتابیس** — `P1` · `M`
  - خروجی: رمزنگاریِ ستون‌های حساس (در صورتِ شناسایی) در سطحِ برنامه.

- [ ] **B4. رمزنگاریِ کلِ دیسک (LUKS) — دفاع در عمق** — `P1` · `S`
  - خروجی: پارتیشنِ دادهٔ سرور رمزنگاری‌شده باشد تا سرقتِ فیزیکیِ دیسک داده را لو ندهد.

- [ ] **B5. اجبارِ TLS/HTTPS و امنیتِ انتقال** — `P1` · `S`
  - خروجی: HTTPS اجباری، هدرهای امنیتی (HSTS و…)، و بازبینیِ فایل‌های عمومیِ سرو‌شونده (آواتار/برندینگ).

---

## گروه C — سکوی دیتابیس (تصمیمِ استراتژیک)

- [ ] **C1. تصمیم‌گیریِ SQLite در برابر Postgres** — `P1` · `S` (تصمیم) / `L` (اجرا)
  - زمینه: Postgres مزیتِ **Replicationِ زنده + PITR** می‌دهد (عملاً صفر داده‌ی از دست‌رفته) که مستقیماً به هدفِ اطمینانِ داده کمک می‌کند.
  - هزینه: کدِ فعلی همگام (`node:sqlite`) است؛ مهاجرت یعنی async شدنِ همه‌ی کوئری‌ها + تغییرِ گویشِ SQL — هرچه دیرتر، سخت‌تر.
  - خروجی: تصمیمِ مکتوب (بله/خیر) + زمان‌بندی.

- [ ] **C2. ساختِ لایه‌ی دسترسی به داده (DAL)** — `P1` · `L` · پیش‌نیازِ مهاجرت
  - خروجی: جداسازیِ کوئری‌ها پشتِ یک لایه، تا مهاجرت به Postgres کم‌ریسک شود.

- [ ] **C3. مهاجرت به Postgres** — `P2` · `L` · وابسته به C1/C2
  - خروجی: مهاجرتِ اسکیما + داده، connection pooling، و راه‌اندازیِ replication/PITR.

- [ ] **C4. سخت‌سازیِ SQLite (اگر ماندیم)** — `P1` · `S`
  - خروجی: checkpointِ منظمِ WAL، graceful shutdown برای جلوگیری از فسادِ WAL هنگام ری‌استارت.

- [ ] **C5. بکاپِ سطحِ Postgres با PITR** — `P1` · `M` · وابسته به C3
  - خروجی: آرشیوِ WAL + بکاپِ افزایشی با **pgBackRest/WAL-G** روی مقصدِ خارج از سرور (S3) → بازیابیِ نقطه‌به‌نقطه با RPO نزدیکِ صفر.
  - قبولی: بتوان دیتابیس را به «یک لحظه‌ی مشخص در گذشته» بازگرداند.

- [ ] **C6. Streaming Replication و آمادگیِ Failover** — `P2` · `M` · وابسته به C3
  - خروجی: یک نسخه‌ی داغِ standby؛ در صورتِ خرابیِ اصلی، جایگزینی سریع.

- [ ] **C7. Connection Pooling (PgBouncer)** — `P1` · `S` · وابسته به C3
  - خروجی: مدیریتِ اتصال‌ها برای Node تا تحتِ بار، اتصال‌ها تمام نشوند.

---

## گروه D — مهاجرتِ اسکیمای دیتابیس  ⭐ (تضمینِ «داده درست، به‌روز، بدونِ از دست رفتن»)

> **قراردادِ ایمنیِ مهاجرت:** هر تغییرِ اسکیما یا کامل و تأییدشده اعمال می‌شود،
> یا اصلاً اعمال نمی‌شود و پشتیبانِ سالمِ پیش از آن در دسترس است. هیچ حالتِ نیمه‌مهاجرت‌شده وجود ندارد.

- [ ] **D1. موتورِ Migrationِ نسخه‌دار** — `P0` · `M`
  - مشکل: الان با `try { ALTER TABLE } catch {}` بدونِ نسخه و ترتیبِ تضمین‌شده مدیریت می‌شود؛ بعد از سال‌ها شکننده و خطرناک است.
  - خروجی: جدولِ `schema_migrations` + فایل‌های مهاجرتِ شماره‌دار که در لحظه‌ی بالا آمدن، مهاجرت‌های معلق را **به ترتیب و دقیقاً یک‌بار** اجرا می‌کند.
  - قبولی: افزودنِ قابلیتِ جدید فقط یک فایلِ مهاجرتِ تازه لازم داشته باشد؛ اجرا خودکار و تکرارناپذیر باشد.

- [ ] **D2. پشتیبانِ خودکارِ پیش از مهاجرت** — `P0` · `S` · وابسته به D1، A1
  - خروجی: قبل از اعمالِ هر مهاجرتِ معلق، خودکار یک پشتیبانِ کامل گرفته شود = نقطه‌ی بازگشت.
  - قبولی: در صورتِ خرابی، بازگشت به وضعیتِ پیش از مهاجرت ممکن باشد.

- [ ] **D3. تراکنش + برگشتِ خودکار در خطا (Atomicity)** — `P0` · `S` · وابسته به D1
  - خروجی: هر مهاجرت داخلِ یک تراکنش؛ خطا → rollback و دیتابیس دست‌نخورده. سامانه روی اسکیمای معیوب **بالا نیاید**.

- [ ] **D4. اعتبارسنجیِ خودکارِ پس از مهاجرت** — `P0` · `S` · وابسته به D1
  - خروجی: اجرای `PRAGMA integrity_check` و `PRAGMA foreign_key_check` پس از هر مهاجرت؛ شکست = توقفِ راه‌اندازی با پیامِ دقیق.

- [ ] **D5. checksum و محافظت از دست‌کاریِ مهاجرت‌های اجراشده** — `P1` · `S` · وابسته به D1
  - خروجی: ثبتِ checksumِ هر فایلِ مهاجرت؛ اگر فایلی که قبلاً اجرا شده تغییر کند، هشدار داده شود.

- [ ] **D6. قانونِ «افزایشی» و الگویِ امنِ تغییراتِ مخرب** — `P1` · `S`
  - خروجی: تغییراتِ افزایشی (add) پیش‌فرض؛ تغییرِ مخرب (drop/rename) فقط با الگویِ «بساز→کپی→جابه‌جا» و بازبینی، نه درجا.

- [ ] **D7. تستِ خودکار برای هر مهاجرت** — `P1` · `M` · مرتبط با G1
  - خروجی: هر مهاجرت روی داده‌ی نمونه اجرا و حفظِ تعداد/مقدارِ رکوردها اثبات شود (تستِ «چیزی گم نمی‌شود»).

---

## گروه E — سخت‌سازیِ امنیت

- [ ] **E1. محدودیتِ نرخ روی لاگین (Rate limiting)** — `P0` · `S`
  - مشکل: نبودِ محدودیت → آسیب‌پذیر به حمله‌ی حدسِ رمز.

- [ ] **E2. ابطال/تازه‌سازیِ توکن (Token revocation/refresh)** — `P1` · `M`
  - مشکل: توکنِ ۷ روزه غیرقابل‌ابطال است؛ لو رفتنش تا ۷ روز خطر دارد.
  - خروجی: نشستِ سمتِ سرور یا لیستِ ابطال + توکنِ کوتاه‌عمر با refresh.

- [ ] **E3. ورودِ دومرحله‌ای برای مدیران** — `P2` · `M`

- [ ] **E4. سیاستِ رمز عبور** — `P2` · `S`
  - خروجی: حداقلِ پیچیدگی، انقضای اختیاری، جلوگیری از رمزهای رایج.

- [ ] **E5. لاگِ حسابرسیِ رویدادهای امنیتی** — `P1` · `M`
  - خروجی: ثبتِ ورود/خروج/تغییرِ دسترسی/دیدنِ اسنادِ حساس (چه کسی، چه زمانی، از کجا).

---

## گروه F — یکپارچگی و بایگانیِ قانونیِ اسناد

- [ ] **F1. حذفِ نرم (Soft-delete) و تغییرناپذیریِ اسنادِ نهایی** — `P1` · `M`
  - مشکل: `ON DELETE CASCADE` اجازه‌ی حذفِ رکوردها را می‌دهد؛ درخواست‌های تأییدشده حکمِ سند دارند.
  - خروجی: جلوگیری از حذفِ اسنادِ نهایی‌شده + نگه‌داریِ کاملِ تاریخچه‌ی اقدامات.

- [ ] **F2. ردِ حسابرسیِ کاملِ تغییراتِ داده** — `P1` · `M`
  - خروجی: لاگِ تغییرات روی رکوردهای کلیدی (که/چه/کِی).

- [ ] **F3. مهر زمانیِ معتبر و کدِ رهگیریِ استعلام‌پذیر** — `P2` · `M`
  - خروجی: تضمینِ اصالتِ اسنادِ چاپی/بایگانی‌شده (فراتر از متنِ فعلیِ پاورقی).

---

## گروه G — تست و CI

- [ ] **G1. تست‌های خودکارِ بک‌اند** — `P1` · `L`
  - خروجی: تست برای منطقِ حساس (گردش‌کار، دسترسی‌ها، نیابت، پشتیبان‌گیری).

- [ ] **G2. خطِ لوله‌ی CI** — `P1` · `M`
  - خروجی: اجرای خودکارِ تست‌ها و بیلد پیش از استقرار.

---

## گروه H — رصدپذیری و عملیات

- [ ] **H1. لاگ‌گیریِ ساختارمند + ثبتِ خطا** — `P1` · `M`
  - مشکل: فقط `console.log` است؛ خطاها گم می‌شوند.

- [ ] **H2. مسیرِ سلامت (`/health`) و متریک‌ها** — `P1` · `S`
  - خروجی: پایشِ در دسترس بودن + وضعیتِ منابع.

- [ ] **H3. هشدار و پایشِ سرویس** — `P1` · `M`
  - خروجی: اطلاع هنگامِ قطعیِ سرویس، پرشدنِ دیسک، یا شکستِ پشتیبان‌گیری.

- [ ] **H4. Graceful shutdown و مدیریتِ فرایند** — `P1` · `S`
  - خروجی: بستنِ امنِ DB/سوکت/زیرفرایندِ LiveKit هنگامِ ری‌استارت.

- [ ] **H5. مدیریتِ رشدِ ذخیره‌سازی** — `P2` · `S`
  - خروجی: سیاستِ آرشیو/پاک‌سازیِ ضبط‌های حجیم تا دیسک پر نشود.

---

## گروه I — مستندسازی و بازیابی از فاجعه (DR)

- [ ] **I1. سندِ DR با تعریفِ RPO/RTO** — `P1` · `S`
  - خروجی: مشخص‌کردنِ «چقدر داده مجاز است از دست برود» و «چقدر سریع برمی‌گردیم».

- [ ] **I2. راهنمای عملیاتی (Runbook)** — `P2` · `M`
  - خروجی: رویه‌های مکتوبِ پشتیبان‌گیری/بازیابی/به‌روزرسانی/چرخشِ کلید برای تیمِ نگه‌داری.

- [ ] **I3. مدیریتِ وابستگی‌ها و به‌روزرسانیِ امنیتی** — `P2` · `S`
  - خروجی: پین‌کردنِ نسخه‌ی Node، بررسیِ دوره‌ای آسیب‌پذیریِ پکیج‌ها.

---

## گروه J — مقیاس‌پذیری، کارایی و آرشیوِ حجیم  (میلیون‌ها ردیف · ترابایت‌ها فایل)

> اصلِ راهنما: **دادهٔ ساختاریافته در دیتابیس، فایل‌های حجیم در object storage.**
> دیتابیس باید سبک بماند؛ ترابایت‌ها هرگز داخلِ آن نروند.

- [ ] **J1. انتقالِ فایل‌های حجیم به Object Storage (S3/MinIO)** — `P1` · `L` · مهم‌ترین تغییرِ مقیاس
  - مشکل: ضبط‌ها و فایل‌های اشتراکی روی دیسکِ محلی‌اند (`data/recordings`, `data/sharedfiles`)؛ در حدِ ترابایت غیرقابل‌مدیریت می‌شود.
  - خروجی: فقط کلید/متادیتا در DB؛ خودِ فایل در object storage با نسخه‌بندی/replication/چرخهٔ‌عمرِ خودش. یک لایهٔ انتزاعِ ذخیره‌سازی تا تعویضِ backend آسان شود.
  - قبولی: بکاپ و رشدِ فایل‌ها مستقل از دیتابیس مدیریت شود.

- [ ] **J2. پارتیشن‌بندیِ زمانیِ جدول‌های پرحجم** — `P1` · `M`
  - خروجی: پارتیشنِ ماهانه برای چت، `workflow_actions`، اعلان‌ها → کوئریِ سریع + آرشیو/حذفِ ارزانِ داده‌ی قدیمی (drop partition).

- [ ] **J3. صفحه‌بندی و استریمِ APIها** — `P1` · `M`
  - مشکل: برخی endpointها کلِ لیست را یک‌جا می‌خوانند (تسک‌ها، اعلان‌ها، پیام‌ها).
  - خروجی: صفحه‌بندیِ سمتِ سرور + بارگذاریِ تنبل در فرانت.

- [ ] **J4. ایندکس‌گذاری و بودجهٔ کاراییِ کوئری** — `P1` · `M`
  - خروجی: ایندکسِ منطبق بر الگوی کوئری، لاگِ کوئریِ کند، بازبینیِ `EXPLAIN ANALYZE`.

- [ ] **J5. لایهٔ کش و مقیاسِ افقی با Redis** — `P2` · `L`
  - خروجی: Redis برای کش، presence، و **adapterِ Socket.io** تا چند instanceِ اپِ بدونِ‌حالت هم‌زمان کار کنند.

- [ ] **J6. Read Replica برای بارِ خواندن** — `P2` · `M` · وابسته به C6
  - خروجی: هدایتِ گزارش‌ها/جستجوی سنگین به replica.

- [ ] **J7. موتورِ جستجوی اختصاصی (در صورت نیاز)** — `P2` · `M`
  - خروجی: full-text خودِ Postgres یا Meilisearch/OpenSearch اگر جستجوی چت/سراسری سنگین شد.

- [ ] **J8. صفِ کار و Worker برای کارهای سنگین/غیرهم‌زمان** — `P2` · `L`
  - خروجی: جداکردنِ پردازشِ ضبط، ارسالِ انبوهِ اعلان و… از فرایندِ اصلی.

- [ ] **J9. سیاستِ نگه‌داری و آرشیوِ سرد (Retention/Cold Storage)** — `P2` · `M`
  - خروجی: انتقالِ چت/ضبطِ خیلی قدیمی به ذخیره‌سازیِ ارزان‌تر؛ جلوگیری از رشدِ بی‌پایانِ داغ.

- [ ] **J10. تستِ بار و برنامه‌ریزیِ ظرفیت** — `P1` · `M`
  - خروجی: seed کردنِ میلیون‌ها ردیف و اندازه‌گیریِ کارایی؛ تعریفِ آستانه‌ی هشدارِ تأخیر/دیسک/replication lag. **تضمینِ کندنشدن با داده، نه حدس.**

- [ ] **J11. استقرارِ مقیاس‌پذیر (Container/IaC)** — `P2` · `L`
  - خروجی: کانتینری‌سازی + زیرساختِ کدمحور برای راه‌اندازیِ چنداینستنسِ تکرارپذیر و مدیریتِ آسان.

---

### پیشنهادِ نقطه‌ی شروع (نظرِ من)
اگر بخواهم اولویتِ خالص بدهم: **A1 → A2 → A3 → A4** (بیمه‌ی داده)، سپس **B1 + B2** (رمزنگاریِ امضاها)، سپس **E1 + D1**.

دو تصمیمِ استراتژیک را هرچه زودتر بگیریم چون بقیه را جهت می‌دهند:
- **C1 (Postgres)** — سکوی دیتابیس و مسیرِ بکاپ/replication را مشخص می‌کند.
- **J1 (Object Storage برای فایل‌ها)** — مهم‌ترین تغییرِ معماری برای رسیدن به ترابایت‌ها؛ هرچه زودتر، مهاجرتش ارزان‌تر.

**فلسفهٔ رشد:** بازنویسی لازم نیست؛ تکاملِ افزایشی در طولِ مرزهای تمیز کافی است. اما دو مرزِ اصلی — «دادهٔ ساختاریافته ↔ فایلِ حجیم» و «اپِ بدونِ‌حالت ↔ حالتِ مشترک (DB/Redis/Storage)» — را باید زود تثبیت کنیم تا مقیاس‌دادن بعداً فقط «افزودنِ اینستنس» باشد.
